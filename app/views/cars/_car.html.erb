<tr>
  <td><%= car.id %></td>
  <td><%= car.number %></td>
  <td><%= car.type %></td>
  <td><%= link_to 'Show',  car_path(car) %></td>
  <td><%= link_to 'Edit', edit_car_path(car) %></td>
  <td><%= link_to 'Destroy',  car_path(car), method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>